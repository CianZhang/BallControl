# 滚球控制系统 PID 调参指南

本指南以仓库当前代码为准，目的是把小球稳定停在目标点，并在此基础上完成路径运动。请一次只改一个轴、一个参数；每次改动都记录结果。

## 1. 先理解这份工程的 PID

`STM32RCT6/hardware/PID.c` 的计算为：

```text
error      = setpoint - feedback
integral   = clamp(integral + error, -600, 600)
derivative = error - previous_error
output     = Kp × error + Ki × integral + Kd × derivative
```

它没有乘、除采样周期 `dt`。因此 `Ki` 和 `Kd` 的数值只对当前的相机帧率、串口传输和主循环执行频率有效。不要把教科书或其他工程的 PID 数值直接照搬。

控制在 `Servo_move_to_traget_close_loop()` 中串联：

```text
位置目标 - 当前像素位置 ─► p_pid（位置外环） ─► 期望帧间位移
期望帧间位移 - dx/dy ─────► v_pid（内环） ───► 舵机角度增量
```

这里 `dx/dy` 由 OpenMV 计算为“本帧像素坐标 - 上帧像素坐标”。它能反映运动方向和快慢，但并不是带单位时间的真实速度。

当前起始参数如下，应将它们记录为可回退的基线：

| 环路 | X 轴 `Kp, Ki, Kd` | Y 轴 `Kp, Ki, Kd` |
| --- | --- | --- |
| 位置外环 `p_pid` | `0.1, 0.0016, 0.08` | `0.1, 0.00255, 0.08` |
| 位移/速度内环 `v_pid` | `0, 0, 1.1` | `0, 0, 1.1` |

## 2. 调参前检查（不通过就不要调 PID）

1. **水平零点**：运行 `main.c` 中的 `Servo_move_by_degress(75, 55)` 后，木板应尽量水平。若球静止时总往固定方向滚，先校准这两个初始角度。
2. **轴向和符号**：给单轴极小的增量，观察木板倾斜和小球运动方向。方向反了，修改 `Servo.c` 的 `servo_rotation_direction` 或 `servo_pitch_direction`。
3. **视觉与通信稳定性**：确认 ROI 覆盖全板，光照不闪烁，色块不会丢失或跳到反光点。还要确认 STM32 中的 `points_x/y`、`dx/dy` 会随每一帧持续更新。OpenMV 初次识别时 `last_points_x/y` 为 0，第一帧的 `dx/dy` 可能很大，应忽略该瞬态。
4. **机械余量**：中间支撑必须稳；舵机连杆不得卡滞。舵机限位是 10°–150°，调试时预留足够余量。
5. **单轴测试**：先将另一轴保持水平，仅调 X；X 稳定后再调 Y。两轴一起调会掩盖问题。

## 3. 推荐调参顺序

### 第一步：只调内环的 Kd，让球能“刹住”

先暂时将位移/速度内环的目标固定为 0。`Servo.c` 中已经保留了测试代码：

```c
// 正常串级控制：先注释这两行
PID_SetSetpoint(&v_pid_x, control_signal_x);
PID_SetSetpoint(&v_pid_y, control_signal_y);

// 内环制动测试：临时启用这两行
PID_SetSetpoint(&v_pid_x, 0);
PID_SetSetpoint(&v_pid_y, 0);
```

然后将内环 `Kp = 0`、`Ki = 0`，只保留 `Kd`。从远小于当前 `1.1` 的保守值开始，逐次小幅增大：手动将球轻推后，观察它能否被木板逐渐刹停。

| 现象 | 判断 | 调整 |
| --- | --- | --- |
| 球继续沿原方向滚，制动很弱 | 阻尼不足 | 小幅增大内环 `Kd` |
| 球在停止前后反复左右摆，舵机抖动 | 阻尼过强，或位移噪声大 | 减小 `Kd`；先改善识别、帧率和机械间隙 |
| 球被推后能平顺减速，只有轻微回摆 | 内环合适 | 记录此值，进入外环 |

内环调好后，恢复 `control_signal_x/y` 作为 `v_pid` 的目标。

> 不要一开始就加内环 `Ki`。相邻帧位移存在量化和噪声，积分很容易放大舵机抖动。

### 第二步：位置外环只调 Kp，让球能到目标附近

保持内环参数不动，先把位置外环 `Ki = 0`、`Kd = 0`。从小 `Kp` 开始，给定固定目标点，逐步增大 `Kp`。

| 现象 | 判断 | 调整 |
| --- | --- | --- |
| 靠近目标很慢，或推不动球 | 位置指令太弱 | 增大位置 `Kp` |
| 快到目标时明显冲过头、来回摆 | 位置环过激，或内环阻尼不足 | 先略减位置 `Kp`；若仍明显，回到第一步增加有效阻尼 |
| 能较快到达目标附近，但有小幅过冲 | 合格的 P 基础 | 加入位置 `Kd` 微调 |

### 第三步：少量加入位置外环 Kd，降低过冲

小幅增加位置外环 `Kd`（当前基线为 `0.08`）。它针对的是位置误差的变化，通常可减少到点前的冲过头。

- 过冲减小且响应仍顺滑：保留或微增。
- 开始出现高频抖动、舵机频繁反向：减小 `Kd`；视觉坐标跳变时尤其要小。
- 若调大 `Kd` 没有效果，优先检查内环和 `dx/dy` 是否可信，而不是无限加大。

### 第四步：最后才加位置外环 Ki，消除静差

只有在系统已经稳定、但始终停在目标点一侧时，才从极小的 `Ki` 开始增加。当前代码的积分限幅为 `±600`，而且每次主循环都会积分；`Ki` 对循环频率十分敏感。

| 现象 | 调整 |
| --- | --- |
| 长期有固定偏差，且没有持续振荡 | 极小步增大位置 `Ki` |
| 到点后缓慢越滚越远、停住后又突然冲出 | `Ki` 过大或发生积分饱和，减小 `Ki` 并清零积分后重试 |
| 目标点切换后出现很大冲击 | 上一目标积累的积分未释放，减小/禁用 `Ki`，或在切换目标时清积分 |

对于本项目，更实用的积分策略是：只有在球接近目标时才允许位置积分；离目标较远时不累计。这能避免长距离运行时的积分堆积，再用较小积分修正木板轻微不平或舵机零偏。

## 4. 一套可执行的测试流程

1. 记录当前四个 PID 的六个参数、零点角度、相机帧率和测试目标坐标。
2. 只开一个轴，完成内环 `Kd` 的推球制动测试。
3. 以木板中心为目标，完成外环 `Kp`、`Kd`、`Ki` 调整。
4. 把目标从中心切换到四角和边缘点，观察是否撞舵机限位、是否沿错误方向加速。
5. 用 `ball_control.c` 中相邻的中间点逐段跑路径；不要直接以大跨度目标检验全部性能。
6. X/Y 分别完成后，再进行二维路径测试。若二维时才变差，通常是结构耦合或支撑不稳，不是简单加大 PID 能解决的。

每次只改一个参数，建议记录：

| 日期/版本 | 轴 | 环路 | Kp | Ki | Kd | 目标点 | 结果（超调、稳定时间、静差、抖动） |
| --- | --- | --- | ---: | ---: | ---: | --- | --- |
|  | X | 内环 | 0 | 0 |  | 0 位移 |  |
|  | X | 外环 |  | 0 | 0 | 中心点 |  |

## 5. 代码相关的注意事项

- 当前串口收包代码存在一项需要先修复的风险：OpenMV 的 `ustruct.pack("bbhhhhbbb", ...)` 在包头 `0xAA, 0xAE` 后发送 11 字节（4 个 16 位数、2 个方向字节、1 个包尾），但 STM32 的 `OpenMV_data` 定义为 10，`UART3_receive_data[10]` 会越界写入。解析到首个包尾后又把 `RxState` 置为 3，等待下一个 `0xAC`；下一帧从 `0xAA` 开始，因此无法正常回到收包状态。**在此问题修正且连续收帧验证通过前，不要根据内环表现调整 PID。**
- `PID_Update()` 在前台 `while` 循环中执行，而 OpenMV 数据只在串口收到新帧后改变。若同一帧数据被重复调用多次，积分会被重复累加，微分也不再代表相邻图像帧变化。这是当前 `Ki` 必须很小的主要原因。
- `dx/dy` 是帧间像素差。帧率变化时，同样的真实速度会得到不同的数值，内环 `Kd=1.1` 也必须重新验证。
- 舵机 PWM 输出虽然被 `Servo_move_by_degress()` 限制，但 `servo_rotation_value`、`servo_pitch_value` 本身仍持续累加。长期撞限位后回程可能变慢或失控；发现撞限位应立即降低增益或缩短目标步长。
- 位置目标在 `ball_control.c` 中已根据实际木板做过坐标修正。若物理格点和画面坐标不重合，应先修正 `points[]`，不要仅靠增大 `Ki`。

## 6. 后续改进优先级

以下改进会让 PID 更可重复，但需要修改控制代码；完成当前调参后再实施即可。

1. 只在收到一帧完整 OpenMV 数据后执行一次 PID，并测量实际 `dt`。
2. 用 `velocity = (position - last_position) / dt` 替代纯帧间位移，并对速度做低通滤波。
3. 对 PID 输出限幅，并在输出饱和时停止同方向积分（抗积分饱和）。
4. 在目标切换、丢球、进入舵机限位时清除积分和误差历史。
5. 增加“目标附近才积分”的误差阈值，并记录位置、位移、PID 输出和舵机角度以便画曲线分析。

这些改动的收益通常大于继续微调小数点后更多位的 PID 参数。
